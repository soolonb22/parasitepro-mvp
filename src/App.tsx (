import React from 'react';
import { BrowserRouter as Router, Routes, Route, Link } from 'react-router-dom';
import ParaGuide from './components/ParaGuide';

function App() {
  return (
    <Router>
      <div className="min-h-screen bg-slate-50">
        {/* Navbar */}
        <nav className="bg-white border-b sticky top-0 z-50">
          <div className="max-w-7xl mx-auto px-6 py-5 flex items-center justify-between">
            <div className="flex items-center gap-3">
              <div className="w-9 h-9 bg-teal-600 rounded-2xl flex items-center justify-center text-white font-bold">P</div>
              <span className="text-2xl font-semibold text-navy">notworms</span>
            </div>
            <div className="flex items-center gap-8 text-sm font-medium">
              <Link to="/" className="hover:text-teal">Home</Link>
              <Link to="/upload" className="hover:text-teal">Analyse</Link>
              <Link to="/pricing" className="hover:text-teal">Pricing</Link>
              <Link to="/dashboard" className="hover:text-teal">Dashboard</Link>
              <ParaGuide variant="inline" />
            </div>
          </div>
        </nav>

        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/upload" element={<UploadPage />} />
          <Route path="/pricing" element={<PricingPage />} />
          <Route path="/dashboard" element={<DashboardPage />} />
        </Routes>
      </div>
    </Router>
  );
}

// Placeholder pages (expand these later)
const Home = () => (
  <div className="max-w-7xl mx-auto px-6 pt-20">
    <h1 className="text-5xl font-semibold text-navy">Found something concerning in your stool?</h1>
    <p className="mt-4 text-xl">Get fast AI-powered analysis in 60 seconds.</p>
    <ParaGuide variant="hero" />
  </div>
);

const UploadPage = () => <div className="p-10">Upload Page - Add Human/Pet toggle here</div>;
const PricingPage = () => <div className="p-10">Pricing Page</div>;
const DashboardPage = () => <div className="p-10">Dashboard Page</div>;

export default App;
