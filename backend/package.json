# Copy the downloaded file into your repo
cp ~/Downloads/package.json /path/to/parasitepro-mvp/backend/package.json

cd /path/to/parasitepro-mvp/backend
npm install   # regenerates package-lock.json

git add package.json package-lock.json
git commit -m "fix: add sharp and @types/sharp — resolve TS2307 build failure"
git push origin main
